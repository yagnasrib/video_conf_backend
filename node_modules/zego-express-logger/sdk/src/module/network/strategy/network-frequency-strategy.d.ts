import { LogStrategy } from './log-strategy';
/**
 * 默认策略
 * 当前是每隔 10ms 上报一次 20KB
 * bind width = 2MB / s
 */
export declare class NetworkFrequencyStrategy extends LogStrategy {
    protected maxDelay: number;
    protected maxPacketLen: number;
    protected locked: boolean;
    canSendLog(): boolean;
    getLog(): string[];
    onSendLog(): void;
}
