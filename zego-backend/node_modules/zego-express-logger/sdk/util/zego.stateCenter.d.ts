/****
 *
 * 负责对接全局状态变化,单独拿出来是为了和全局状态做切割,清晰的看到日志里面用到了哪些全局变量
 *
 * **/
export declare class LoggerStateCenter {
    static instance?: LoggerStateCenter;
    constructor();
    static getInstance(): LoggerStateCenter;
    /**
     *  监听全局状态变化
     * **/
    private _init;
    private _testEnvironment;
    private _ENV;
    get testEnvironment(): boolean;
    set testEnvironment(env: boolean);
    get ENV(): number;
    set ENV(env: number);
    private _debug;
    get debug(): boolean;
    set debug(debug: boolean);
}
